import unknownSongImage from "@/assets/images/unknown_song.jpg"
import { Image } from "react-native"

export const unknownTrackImageUri = Image.resolveAssetSource(unknownSongImage).uri

export default function(){};